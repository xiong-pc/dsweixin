# =============================================================================
# Nuxt 3 商城前台环境变量（M11-PR42）
# 复制为 .env 后按需修改。NUXT_PUBLIC_* 会暴露到客户端，敏感配置请不要前缀 PUBLIC。
# =============================================================================

# 后端 API base URL（包含 /api/v1 前缀，Nuxt 通过 useApi 直接拼路径）
NUXT_PUBLIC_API_BASE=http://localhost:8000/api/v1

# 子域解析 header：本地开发主域不是真实子域时，用此 header 告诉后端走哪个 shop
NUXT_PUBLIC_SHOP_HEADER=X-Shop-Subdomain

# 平台主域（生产环境每个店铺挂 *.example.com 子域 → 后端 ShopResolverMiddleware 按 host 解析）
NUXT_PUBLIC_PLATFORM_DOMAIN=platform.local

# 本地开发兜底子域：未配置真实子域时，前端 tenant.global.ts 会主动加 header
NUXT_PUBLIC_FALLBACK_SUBDOMAIN=demo-shop
